# 初始化驗證清單

> 版本：v0.4
> 更新日期：2026-06-06

每次新專案初始化或既有專案補初始化完成後，用這份清單做最後檢查。

跨設備使用時，本清單中的 `CODEX_HOME`、`OBSIDIAN_VAULT_ROOT`、`PROJECTS_ROOT` 依各設備本機路徑決定。規則請看 `跨設備路徑規則.md`。

---

## 基本資料

- [ ] 專案顯示名稱已確認
- [ ] 專案代號已確認
- [ ] 工作根目錄已確認
- [ ] GitHub repo slug 已確認
- [ ] Obsidian vault 路徑已確認
- [ ] Firebase 專案名稱已確認或標記為空白
- [ ] Windows / Linux / NAS 路徑對照已確認
- [ ] 平台支援邊界已確認
- [ ] 若本次只補缺少的狀態檔，已標記為低風險狀態檔初始化

---

## 專案檔案

- [ ] 專案根目錄存在 `README.md`
- [ ] 專案根目錄存在 `CLAUDE.md`
- [ ] 專案根目錄存在 `current-state.md`
- [ ] 專案根目錄存在 `handoff.md`
- [ ] 專案根目錄存在 `worklog.md`
- [ ] 專案根目錄存在 `runbook.md`
- [ ] 專案根目錄存在 `tasks.md`
- [ ] 專案根目錄存在 `.gitattributes`
- [ ] 專案根目錄存在 `.gitignore`
- [ ] 專案根目錄存在 `tools/` 或已確認目前不需要
- [ ] `.gitignore` 已排除 `.claude/`
- [ ] `.gitignore` 已排除 `.codex/`
- [ ] `.gitignore` 已排除 `.env`
- [ ] `.gitignore` 已排除 `.project-session/`
- [ ] `.gitignore` 已排除 venv、build、cache、models、outputs 等大型或生成資料
- [ ] `.gitignore` 已排除金鑰與憑證類檔案

---

## CLAUDE.md

- [ ] 有 `專案識別` 區塊
- [ ] `專案代號` 正確
- [ ] `工作根目錄` 正確
- [ ] `Linux 對應路徑` 正確或已標記為空白
- [ ] `GitHub repo` 正確
- [ ] `Obsidian 工作筆記` 使用 `專案庫/<專案代號>/工作筆記.md`
- [ ] 沒有把 GitHub repo slug 和專案顯示名稱混用
- [ ] 有寫明 `CLAUDE.md` 是主要依據
- [ ] 有寫明平台支援邊界
- [ ] 有列出專案內狀態檔

---

## 專案內狀態檔

- [ ] `current-state.md` 有最後動作、目前狀態、下一步
- [ ] `handoff.md` 有可讓新對話或另一台電腦接續的指令
- [ ] `worklog.md` 有初始化紀錄
- [ ] `tasks.md` 有待辦、決策、阻塞區塊
- [ ] `runbook.md` 有啟動、測試、部署或標記目前不需要
- [ ] `.project-session/active-session.json` 若存在，確認未進版控
- [ ] 低風險狀態檔初始化時，只新增或更新缺少的狀態管理檔

---

## Obsidian

- [ ] 工作筆記位於 `專案庫/<專案代號>/工作筆記.md`
- [ ] 工作筆記中的 repo 連結可用
- [ ] `上次做到哪` 有目前進度
- [ ] `最近更動紀錄` 有初始化紀錄
- [ ] `工具清單` 和 `CLAUDE.md` 一致

---

## Git 與 GitHub

- [ ] `.git` 存在
- [ ] `git status --short` 可正常執行
- [ ] 若出現 `dubious ownership`，已設定該專案的 `safe.directory`
- [ ] remote 清單已確認
- [ ] 目前 branch 已確認
- [ ] branch tracking 已確認
- [ ] GitHub repo URL 可開啟
- [ ] 初始化 commit 已完成或已確認不需要

---

## Codex 工作流程

- [ ] `startup` 會先讀目前專案的 `CLAUDE.md`
- [ ] `startup` 能找到正確的專案內狀態檔
- [ ] `startup` 能找到正確的 Obsidian 工作筆記
- [ ] `shutdown` 會先讀目前專案的 `CLAUDE.md`
- [ ] `shutdown` 會更新正確的專案內狀態檔
- [ ] `shutdown` 會更新正確的 Obsidian 工作筆記
- [ ] `shutdown` 不會把 `.claude/`、`.codex/`、`.env`、`.project-session/` 加入版控
- [ ] `startup` 若發現缺少標準狀態檔，會先建議低風險狀態檔初始化
- [ ] `shutdown` 若收斂低風險初始化，commit 範圍只限狀態管理檔與必要工作筆記紀錄

---

## 跨設備檢查

- [ ] Windows 路徑與 Linux 路徑可互相對照
- [ ] Linux 端 Obsidian vault 路徑已確認，或已標記待確認
- [ ] Git `safe.directory` 屬本機設定，未寫成共享規則
- [ ] 若專案在 NAS 上，已評估同時修改衝突風險
- [ ] 若專案無 remote，已明確標記只做本機 / NAS commit
- [ ] 若目前位置是 NAS shared state/document path，已略過 git 操作
- [ ] 若需要 git 同步，已改在每台電腦自己的 local git working copy commit / push

---

## 低風險狀態檔初始化

- [ ] 已讀 `CLAUDE.md`
- [ ] 已讀 Obsidian 工作筆記，或已標記缺少工作筆記
- [ ] 已列出缺少哪些標準狀態檔
- [ ] 只建立或更新 `current-state.md`、`handoff.md`、`tasks.md`、`worklog.md`、`runbook.md`
- [ ] 沒有修改功能程式、腳本、設定檔、`.git`、`.env`、token、憑證或機器專屬設定
- [ ] 若在 NAS path，已說明只寫共享文件、不執行 git
- [ ] 若在 local working copy，`git add` 範圍只包含狀態管理檔
- [ ] 若需要把 NAS 狀態檔納入 git，已先同步到 local working copy 再提交

---

## Stop 備份安全網

- [ ] 安全網只做本機 auto-commit，不自動 push
- [ ] 安全網只處理已追蹤檔案
- [ ] 專案若需要本機自動備份，已加入 `<CODEX_HOME>/project-backup-allowlist.txt`
- [ ] 已確認 log 位置：`<CODEX_HOME>/scripts/session-backup.log`

---

## 完成判定

全部必要項目通過後，才算完成初始化。
