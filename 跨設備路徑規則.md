# 跨設備路徑規則

> 版本：v0.2
> 更新日期：2026-06-05

這份文件處理同一套初始化規則在 Windows、Linux、不同電腦之間同步使用時的路徑問題。

---

## 核心原則

共享文件不要綁定單一電腦的絕對路徑。

也就是說，放在 GitHub、雲端同步資料夾、Obsidian 的共用規則文件，應優先使用：

- 專案內相對路徑
- vault 相對路徑
- 本機環境變數名稱

不要把某一台電腦的實際路徑寫成規則，例如：

```text
G:\Agent\Project\ai-toolkit\初始化驗證清單.md
```

應改成：

```text
<AI_TOOLKIT_ROOT>/初始化驗證清單.md
```

或：

```text
目前 ai-toolkit 專案根目錄下的 初始化驗證清單.md
```

---

## 建議使用的路徑變數

| 變數 | 意義 | Windows 範例 | Linux 範例 |
|------|------|--------------|------------|
| `AI_TOOLKIT_ROOT` | `ai-toolkit` repo 在本機的位置 | `G:\Agent\Project\ai-toolkit` | `/home/altos/nas/Agent/Agent/Project/ai-toolkit` |
| `PROJECTS_ROOT` | 新專案預設放置根目錄 | `G:\Agent\Project` | `/home/altos/nas/Agent/Agent/Project` |
| `OBSIDIAN_VAULT_ROOT` | Obsidian vault 根目錄 | `G:\Agent\2ndbrain\筆記庫` | `<Altos 端待確認>` |
| `CODEX_HOME` | Codex 本機設定根目錄 | `C:\Users\intor\.codex` | `/home/altos/.codex` |
| `ALTOS_WORKSPACES_ROOT` | Altos 本機 Codex workspace 根目錄 | 不適用 | `/home/altos/codex-workspaces` |

這些變數不一定要真的設定成系統環境變數，也可以只是文件中的標準名稱。

目前已確認的 NAS 對照：

| Windows | Linux |
|---|---|
| `G:\Agent\Project` | `/home/altos/nas/Agent/Agent/Project` |

若專案在 Altos 本機 workspace，例如 `/home/altos/codex-workspaces/gb10-aigeni-ops`，它不是 NAS 共享專案本體；要跨 Windows 接續時，應另用 GitHub remote 或搬入 NAS Project 正式專案區。

---

## 共享文件應該怎麼寫

### 專案內文件

使用相對於 `AI_TOOLKIT_ROOT` 的路徑：

```text
README-初始化規則.md
新專案初始化表單.md
初始化驗證清單.md
examples/standard-project/CLAUDE.md
```

### Obsidian 工作筆記

使用相對於 `OBSIDIAN_VAULT_ROOT` 的路徑：

```text
專案庫/<專案代號>/工作筆記.md
```

### 專案內狀態檔

使用相對於專案根目錄的路徑：

```text
current-state.md
handoff.md
worklog.md
tasks.md
runbook.md
```

active session 提醒檔使用：

```text
.project-session/active-session.json
```

`.project-session/` 必須加入 `.gitignore`，不可進版控。

### 本機 Codex 設定

使用相對於 `CODEX_HOME` 的路徑：

```text
<CODEX_HOME>/project-backup-allowlist.txt
<CODEX_HOME>/scripts/session-backup.log
```

---

## 每台設備要各自設定的內容

以下內容屬於本機設定，不應假設每台電腦相同：

- `AI_TOOLKIT_ROOT`
- `PROJECTS_ROOT`
- `OBSIDIAN_VAULT_ROOT`
- `CODEX_HOME`
- `ALTOS_WORKSPACES_ROOT`
- Stop 備份白名單內容
- Git `safe.directory`
- GitHub CLI 登入狀態
- Obsidian vault 實際位置
- GitHub CLI / gh 登入狀態
- Codex skills 安裝位置

---

## 新設備第一次使用流程

1. 找到或 clone `ai-toolkit` repo
2. 確認本機的 `AI_TOOLKIT_ROOT`
3. 確認本機的 `PROJECTS_ROOT`
4. 確認本機的 `OBSIDIAN_VAULT_ROOT`
5. 確認本機的 `CODEX_HOME`
6. 依需要設定 Git `safe.directory`
7. 依需要把專案加入 `<CODEX_HOME>/project-backup-allowlist.txt`

---

## 文件撰寫規則

- 共享文件內優先寫相對路徑
- 只有在「範例」或「本機設定」段落才寫 Windows / Linux 絕對路徑
- 若必須寫絕對路徑，需標明是範例，不是規則
- 初始化模板內應使用 `<AI_TOOLKIT_ROOT>`、`<PROJECTS_ROOT>`、`<OBSIDIAN_VAULT_ROOT>`、`<CODEX_HOME>`、`<ALTOS_WORKSPACES_ROOT>`
