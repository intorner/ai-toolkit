# ai-toolkit Handoff

## 接續指令

先讀：

1. `CLAUDE.md`
2. `current-state.md`
3. `tasks.md`
4. `專案初始化模板.md`
5. `既有專案補初始化SOP.md`
6. `初始化驗證清單.md`

再依 `tasks.md` 的下一步繼續。

## 注意事項

- 本次為低風險狀態檔初始化，只新增狀態管理檔。
- 不要混入功能程式、腳本、設定檔、`.git`、`.env`、token、憑證或機器專屬設定。
- 若要 push，先取得明確授權。
