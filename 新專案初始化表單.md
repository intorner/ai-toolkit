# 新專案初始化表單

> 版本：v0.1
> 更新日期：2026-05-03

建立新專案前，先填這份表單。Codex 會依這些欄位產生 `CLAUDE.md`、`.gitignore`、Obsidian 工作筆記，並協助初始化 GitHub。

---

## 1. 專案基本資料

| 欄位 | 填寫 |
|------|------|
| 專案顯示名稱 |  |
| 專案代號 |  |
| 工作根目錄 |  |
| GitHub 帳號 | `intorner` |
| GitHub repo slug |  |
| Obsidian vault 路徑 | `G:\Agent\2ndbrain\筆記庫` |
| Firebase 專案名稱 |  |

填寫原則：
- `專案顯示名稱` 可以中文，例如 `成績儀表板`
- `專案代號` 建議穩定、可放路徑，例如 `score-dashboard`
- `GitHub repo slug` 以實際 GitHub repo 名稱為準，不要假設等於顯示名稱
- `Firebase 專案名稱` 沒有就留空

---

## 2. Obsidian 路徑推導

填完 `專案代號` 後，工作筆記固定放在：

```text
<Obsidian vault 路徑>\專案庫\<專案代號>\工作筆記.md
```

範例：

```text
G:\Agent\2ndbrain\筆記庫\專案庫\score-dashboard\工作筆記.md
```

---

## 3. GitHub 設定

| 欄位 | 填寫 |
|------|------|
| 是否建立新 GitHub repo |  |
| repo 是否公開 |  |
| 是否啟用 GitHub Pages |  |
| 預設 branch |  |

建議：
- 教學工具若要用 GitHub Pages，repo 通常要公開
- 預設 branch 可用 `main` 或沿用現有 repo 設定

---

## 4. Stop 備份安全網

| 欄位 | 填寫 |
|------|------|
| 是否加入 Stop 備份白名單 |  |

若填「是」，初始化完成後把工作根目錄加入：

```text
C:\Users\intor\.codex\project-backup-allowlist.txt
```

注意：
- Stop 備份只做本機 auto-commit
- 不會自動 push
- 只處理已追蹤檔案

---

## 5. 第一個工具或功能

| 欄位 | 填寫 |
|------|------|
| 第一個工具名稱 |  |
| 第一個工具代號 |  |
| 工具類型 |  |
| 是否需要資料庫 |  |
| 是否需要部署 |  |

範例：
- 第一個工具名稱：`座標獵人`
- 第一個工具代號：`coordinate-hunter`
- 工具類型：`純前端練習遊戲`
- 是否需要資料庫：`否`
- 是否需要部署：`GitHub Pages`

---

## 6. 交給 Codex 的固定句型

正式短口令：

```text
開始新專案
```

同義句：

```text
我要開始新專案
我要初始化新專案
開一個新專案
建立新專案
新專案初始化
```

Codex 聽到以上句型時，應先讀：

```text
G:\Agent\Project\ai-toolkit\新專案初始化表單.md
```

如果表單已填完，Codex 會依表單內容初始化新專案。

如果表單還沒填完，Codex 會引導你把缺的欄位補齊。

完整句型仍可使用：

```text
請依照 G:\Agent\Project\ai-toolkit\新專案初始化表單.md 的內容，幫我初始化這個新專案。
```

補欄位句型：

```text
請依照新專案初始化表單，引導我把缺的欄位補齊。
```

---

## 7. 初始化後檢查

初始化完成後，請依：

```text
G:\Agent\Project\ai-toolkit\初始化驗證清單.md
```

逐項確認。
