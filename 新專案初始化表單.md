# 新專案初始化表單

> 版本：v0.3
> 更新日期：2026-09-18

本表單只收集建立 Repository/project-state bootstrap 所需資料。ChatGPT Project Instructions 由 `codex-sync/docs/chatgpt-project-bootstrap-library.md` 產生，不在本表單複製 shared routing。

## 1. Project identity

| 欄位 | 填寫 |
|---|---|
| 專案顯示名稱 | |
| 專案代號 | |
| Project purpose（1–3句） | |
| 工作根目錄 / local projection（可待解析） | |
| GitHub owner / repo slug（可無 remote） | |
| 是否長期/多對話/decision-rich | |
| 是否會使用 shared AI engineering / GB10 capability | |
| 是否需要 Obsidian/work-note projection（可選） | |
| platform boundary（如有） | |

原則：
- 顯示名稱、專案代號、repo slug 分開。
- local path / Windows/Linux/NAS path 是 projection，不是 authority。
- 不要求 Human 為 shared routing 選 Codex/Hermes/RDC/runner；由 canonical routing解析。
- 不要求 Obsidian 才能初始化。
- 不猜 credential、secret、API key 或 machine-specific auth path。

## 2. Repository state structure

預設建立最小必要：

```text
README.md
CLAUDE.md
current-state.md
handoff.md
worklog.md
runbook.md
tasks.md
.gitignore
.gitattributes
```

`docs/`、`scripts/`、`tools/` 等目錄只在真實需求出現時建立，不預建空 future-facing structure。

長期/多階段 Project 可啟用 Durable Recording，但第一份 checkpoint 真的需要時才建立 history/checkpoint surface。

## 3. Shared capability / GB10

若 Project 會使用 shared AI engineering：
- generated `CLAUDE.md` 只加入 thin `codex-sync` redirect。
- 不把 capability trigger、RDC fallback、model、endpoint、host、runner、port 複製進 consumer repo。

若涉及 GB10：
- redirect 到 `codex-sync/docs/gb10-gpt-bootstrap.md` 與 canonical routing。
- 不在本表單維護 GB10 mutable Current Truth。

## 4. Optional work-note projection

若 Human 確實需要 Obsidian/work note，可記錄 semantic locator，例如：

`專案庫/<專案代號>/工作筆記.md`

實際 Vault root / drive letter屬 machine-local projection，可由 current environment / canonical storage resolution取得；不要把固定 drive當跨設備 authority。

## 5. Git / remote authorization

以下必須分開詢問/確認 authorization：
- local Git init / add / commit
- remote repo create
- remote change
- push

「開始新專案／初始化新專案」只授權建立 project files，不自動授權上述 Git/remote side effects。

## 6. 固定句型

```text
開始新專案
我要開始新專案
我要初始化新專案
建立新專案
新專案初始化
```

Agent 應讀本表單 + current template/spec，能安全推導的欄位自行解析；只有真正缺少 Human judgement 的資訊才詢問。

## 7. 驗證

完成後執行 `初始化驗證清單.md`，並確認：
- `current-state.md` 是 Current Truth surface；
- `CLAUDE.md` 只是 bootstrap/control entrypoint；
- shared routing externalized；
- 未擅自執行未授權 Git/remote writes。
