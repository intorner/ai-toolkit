# 專案初始化模板

> 版本：v0.4
> 更新日期：2026-05-03
> 適用範圍：新專案初始化、既有專案補初始化、跨專案 Codex 工作模式

這份模板用來把新專案或既有專案套上同一套「GitHub + Obsidian + Codex」工作模式。

跨設備使用時，請先看 `跨設備路徑規則.md`。共享文件不要綁定單一設備的絕對路徑。

這份模板已根據跨專案實測回饋調整，核心原則如下：

- `CLAUDE.md` 是專案級控制面板
- `startup` / `shutdown` 必須先讀 `CLAUDE.md`，不能寫死單一專案路徑
- Obsidian 專案資料夾固定放在 `專案庫/<專案代號>/`
- `專案顯示名稱`、`專案代號`、`GitHub repo slug` 必須分開
- 新專案初始化與既有專案補初始化要分兩條流程

---

## CLAUDE.md 的地位

`CLAUDE.md` 不只是說明文件，它是這套工作模式的單一真相來源。

它至少提供這些資訊：
- 專案顯示名稱
- 專案代號
- 工作根目錄
- GitHub repo
- Obsidian 工作筆記位置
- 工具清單

規則：
- `startup` 先讀 `CLAUDE.md`，再去找工作筆記
- `shutdown` 先讀 `CLAUDE.md`，再決定要更新哪份工作筆記、同步哪個 repo
- 如果 `CLAUDE.md` 和 `工作筆記.md` 不一致，以 `CLAUDE.md` 為主，再回頭修正工作筆記

---

## 初始化前要填的欄位

只填這組，不再另外讓使用者自由定義 Obsidian 專案資料夾。

| 欄位 | 說明 | 範例 |
|------|------|------|
| 專案顯示名稱 | 給人讀的名稱，可中文 | `成績儀表板` |
| 專案代號 | 給資料夾、路徑、工具使用的穩定名稱 | `score-dashboard` |
| 工作根目錄 | 實際專案位置 | `<PROJECTS_ROOT>/score-dashboard` |
| GitHub 帳號 | GitHub owner | `intorner` |
| GitHub repo slug | 實際 repo 名稱，不假設等於顯示名稱 | `score-dashboard` |
| Obsidian vault 路徑 | vault 根目錄 | `<OBSIDIAN_VAULT_ROOT>` |
| Firebase 專案名稱 | 可空白 | `my-teaching-tools` |

固定推導規則：

- Obsidian 專案資料夾：`<Obsidian vault 路徑>\專案庫\<專案代號>\`
- Obsidian 工作筆記：`<Obsidian vault 路徑>\專案庫\<專案代號>\工作筆記.md`

---

## 初始化順序總覽

### 新專案

1. 建立專案資料夾
2. 建立 `CLAUDE.md`
3. 建立 `.gitignore`
4. 建立 `tools/`
5. 初始化 git 並建立 GitHub repo
6. 建立 Obsidian 工作筆記
7. 驗證 `startup` / `shutdown`

### 既有專案補初始化

1. 確認 `.git` 是否已存在
2. 檢查 remote 與目前 branch 狀態
3. 補 `CLAUDE.md`
4. 補 `.gitignore`
5. 建立或修正 Obsidian 工作筆記
6. 確認 `startup` / `shutdown` 能從 `CLAUDE.md` 解析路徑
7. 做最小初始化 commit

---

## 專案資料夾結構

```text
<工作根目錄>/
  CLAUDE.md
  .gitignore
  tools/
```

如果是既有專案補初始化，至少要補上：
- `CLAUDE.md`
- `.gitignore`
- Obsidian 的 `工作筆記.md`

---

## CLAUDE.md 範本

把下面內容存成專案根目錄的 `CLAUDE.md`，再把尖括號欄位換掉：

```markdown
# <專案顯示名稱>

## 專案識別
- 專案代號：`<專案代號>`
- 工作根目錄：`<工作根目錄>`
- GitHub repo：`<GitHub帳號>/<GitHub repo slug>`
- Obsidian 工作筆記：`專案庫/<專案代號>/工作筆記.md`
- Firebase 專案：`<Firebase 專案名稱>`

## 對話開始時請先讀
進度與最近更動都在 Obsidian：`專案庫/<專案代號>/工作筆記.md`

## 工作模式
- **加新工具**：對 Codex 說「我想做一個 XXX 工具」→ Codex 會建 `tools/<工具名>/` 子資料夾、引導我往下做
- **結束工作**：對 Codex 說「**收工**」→ 先依 `CLAUDE.md` 找到正確的工作筆記與 repo，再進行同步
- **接續工作**：對 Codex 說「讀工作筆記、告訴我上次做到哪」

## 工具清單
- （尚無）

## 工作注意事項
- 學生資料一律去識別化（只用座號 + 班級代號）
- commit 訊息要寫清楚做了什麼 + 為什麼
- 收工前說「收工」讓 Codex 同步三方
- 若 `CLAUDE.md` 與工作筆記不一致，以 `CLAUDE.md` 為主
```

---

## .gitignore 範本

把下面內容存成專案根目錄的 `.gitignore`：

```gitignore
# Cloud sync system files
desktop.ini
*.tmp
~$*

# Sensitive data
.env
*.key
credentials.*

# Local agent settings
.claude/
.codex/

# Node.js
node_modules/
package-lock.json
```

重點：
- `.claude/` 一定不要進版控
- `.codex/` 也不要進版控
- 如果專案還有其他敏感檔案，再另外補上

---

## 工作筆記範本

在 Obsidian 建立：

```text
<Obsidian vault 路徑>\專案庫\<專案代號>\工作筆記.md
```

內容如下：

```markdown
# <專案顯示名稱> 工作筆記

> 進度日誌（變動快）。專案藍圖請看工作目錄的 `CLAUDE.md`。
> 進度只在這裡記錄，避免雙寫漂移。

## 上次做到哪

**最後動作**：剛完成初始化，還沒開始第一個功能。
**所在 repo**：[<GitHub repo slug>](https://github.com/<GitHub帳號>/<GitHub repo slug>)

## 工具清單

（尚無，待加）

## 最近更動紀錄

| 日期 | 變更摘要 | GDrive | Obsidian | GitHub |
|------|----------|--------|----------|--------|
| <今天日期> | 初始化專案工作模式 | OK | OK | OK |

## 踩坑筆記

（之後遇到坑就記在這）
```

---

## 新專案初始化 SOP

### 1. 建立工作目錄

```text
<工作根目錄>
```

### 2. 建立專案基本檔案

- 建 `CLAUDE.md`
- 建 `.gitignore`
- 建 `tools/`

### 3. 初始化 git 與 GitHub

在專案根目錄執行：

```bash
git config windows.appendAtomically false
git init
git add .
git commit -m "初始化專案工作模式"
gh repo create <GitHub repo slug> --public --source=. --push
```

注意：
- `GitHub repo slug` 不一定等於顯示名稱
- 若你打算用中文顯示名稱，repo slug 仍建議用穩定英文或既有可接受名稱

### 4. 建立 Obsidian 工作筆記

固定放在：

```text
<Obsidian vault 路徑>\專案庫\<專案代號>\工作筆記.md
```

---

## 既有專案補初始化 SOP

完整細節請看：`既有專案補初始化SOP.md`

### 1. 確認 git 狀態

至少檢查：
- 是否已有 `.git`
- 是否已有 commit
- 目前 branch 是什麼
- remote 有哪些
- 目前 branch 正在追蹤哪個 remote

### 2. 不要直接覆蓋既有 remote

常見情況：
- repo 已存在
- remote 已存在但名稱不一定是 `origin`
- 本機與遠端可能尚未同步

原則：
- 先盤點現況，再決定是新增 remote、修正 tracking，還是只補初始化檔案
- 不要看到 `origin` 就直接覆蓋

### 3. 補初始化檔案

- 補 `CLAUDE.md`
- 補 `.gitignore`
- 建 `tools/`（若原本沒有）
- 建或搬移 Obsidian `工作筆記.md`

### 4. 若專案搬家，先保主體完整

若專案從舊位置搬到新位置：
- 先確認新位置內容完整
- 若舊位置留下空殼資料夾且暫時刪不掉，不要因此中斷初始化
- 把舊殼刪除視為後續清理項即可

### 5. 做最小初始化 commit

只提交初始化相關檔案，不要順手混入其他大改動。

---

## 技能配合規則

`startup` / `shutdown` 必須遵守：

1. 先找目前專案根目錄
2. 讀該專案的 `CLAUDE.md`
3. 從 `CLAUDE.md` 解析：
   - 專案名稱
   - GitHub repo
   - Obsidian 工作筆記位置
4. 再執行開工或收工流程

禁止：
- 把專案路徑寫死在 skill
- 把單一專案的工作筆記位置寫死在 skill
- 假設 repo slug 一定等於專案名稱

---

## 初始化驗證清單

完整可勾選版本請看：`初始化驗證清單.md`

初始化完成後，至少驗這些：

- `CLAUDE.md` 是否存在且欄位正確
- `startup` 是否能讀到正確工作筆記
- `shutdown` 是否會更新正確工作筆記
- `CLAUDE.md` 內的 GitHub repo 是否對得上真實網址
- `工作筆記.md` 中的 repo 連結是否可用
- 專案工作目錄是否已在預期位置
- `.gitignore` 是否已排除 `.claude/`、`.codex/`、`.env`

---

## 固定口令

之後對 Codex 可以直接用這幾句：

- `我要初始化一個新專案`
- `我要幫既有專案補初始化`
- `我要做一個 XXX 工具`
- `請幫我建立 tools/xxx`
- `讀工作筆記、告訴我上次做到哪`
- `收工`

---

## 常見排錯

### Git 顯示 dubious ownership

如果 Codex 或終端機執行 `git status` 時出現類似訊息：

```text
fatal: detected dubious ownership in repository
```

通常代表該資料夾是由不同 Windows 身分或管理員權限建立。確認路徑正確且是你信任的專案後，針對該專案加入 Git 安全目錄：

```bash
git config --global --add safe.directory <工作根目錄>
```

範例：

```bash
git config --global --add safe.directory G:/Agent/Project/ai-toolkit
```

### GitHub repo slug 和中文專案名稱不同

GitHub 可能會正規化 repo 名稱，所以不要用專案顯示名稱推導網址。請以 `GitHub repo slug` 欄位記錄實際 repo 名稱。

### 舊專案資料夾搬移後刪不掉

先確認新位置內容完整、git 能正常運作。舊位置若只剩空殼且被檔案總管、終端機或同步程式占用，可列為後續清理，不要因此中斷初始化。

---

## 最小套用版

如果你只是想把舊專案快速補齊，最低限度做這 5 件事：

1. 補 `CLAUDE.md`
2. 補 `.gitignore`
3. 建立 `專案庫/<專案代號>/工作筆記.md`
4. 檢查 GitHub remote 與 branch tracking
5. 驗證 `startup` / `shutdown` 是否會先讀 `CLAUDE.md`
